RewriteEngine On

# 1. Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# 2. Define Custom MIME Types & Encodings
# text/javascript is the modern standard (RFC 9239), but x-javascript is fine if you prefer legacy support
AddType application/wasm wasm
AddType 'text/html; charset=UTF-16' 1ink
AddType 'text/javascript; charset=UTF-16' 1ijs
AddType 'text/javascript; charset=UTF-8' js
AddType 'text/css; charset=UTF-16' 1iss

# 3. CRITICAL: Enable Compression for UTF-16
# UTF-16 files consist of 50% null bytes. Gzip/Deflate will shrink them massively.
# We map the compression filter to your custom MIME types.
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE application/wasm
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/javascript
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE application/x-javascript
</IfModule>

# 4. High-Performance Headers
Header set Accept-Ranges bytes
Header set Cross-Origin-Opener-Policy: same-origin
Header always set Cross-Origin-Embedder-Policy: require-corp
Header set Access-Control-Allow-Origin "*"
Header set Cross-Origin-Resource-Policy: cross-origin

# 5. (Optional) Caching
# Since your files are heavy, tell the browser to keep them.
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/javascript "access plus 1 year"
    ExpiresByType application/wasm "access plus 1 year"
    ExpiresByType text/css "access plus 1 year"
</IfModule>
